'use strict';

/**
 * download-categorie service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::download-categorie.download-categorie');
