'use strict';

/**
 * portfolio-domain service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::portfolio-domain.portfolio-domain');
