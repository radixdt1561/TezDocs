'use strict';

/**
 * group-classification service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::group-classification.group-classification');
