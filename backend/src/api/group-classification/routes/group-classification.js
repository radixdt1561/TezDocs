'use strict';

/**
 * group-classification router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::group-classification.group-classification');
