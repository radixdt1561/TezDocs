export default{
    computed:{
        toGradient(){
          var title = this.data.title
          var titleGradient = title.replace(/\*\*(.*?)\*\*/gm, '<strong>$1</strong>')
          return titleGradient;
        }
      },
}